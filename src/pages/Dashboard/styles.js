import styled from 'styled-components';

export const StyledDashboard = styled.div`
    min-height: 100vh;
    justify-content: center;
    align-items: center;
  
    .border-dashboard{
      border: 4px solid var(--borderForm);
      border-radius: 1rem;
    }

  h1 {
    font-size: 24px;
    text-size-adjust: initial;
    color: var(--textH1);
  }
`;
